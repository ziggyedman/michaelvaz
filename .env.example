# Copy to .env and fill in values

# Port to listen on (default: 3001)
PORT=3001

# Comma-separated user list: "username:password,username2:password2"
# Defaults to admin:admin if not set
USERS=admin:changeme
